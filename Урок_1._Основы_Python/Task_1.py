'''
Задача 1

Напишите программу,которая запрашивает год и проверяет его на високосность.
Распишите все возможные проверки в цепочке elif
Обязательно учтите год ввода по Григорианскому календарю 
'''
# Вариант 1:

# Запрос года у пользователя
year = int(input("Введите год для проверки: "))

# Проверяем, является ли год високосным
if year < 1582:
    print(f"Год {year} не соответствует Григорианскому календарю.")
elif year % 400 == 0:
    print(f"Год {year} является високосным.")
elif year % 100 == 0:
    print(f"Год {year} не является високосным.")
elif year % 4 == 0:
    print(f"Год {year} является високосным.")
else:
    print(f"Год {year} не является високосным.")



""" 
# В этой строке мы просим пользователя ввести год и преобразуем его в целое число
year = int(input("Введите год для проверки: "))

# Проверяем, меньше ли введенный год 1582 года, что соответствует началу использования Григорианского календаря
if year < 1582:
    # Если год меньше 1582, то сообщаем пользователю, что этот год не соответствует Григорианскому календарю
    print(f"Год {year} не соответствует Григорианскому календарю.")
# Если год больше или равен 1582, проверяем, является ли он високосным
elif year % 400 == 0:
    # Если год делится на 400 без остатка, то он високосный
    print(f"Год {year} является високосным.")
# Если год не делится на 400, но делится на 100, проверяем, делится ли он еще и на 4
elif year % 100 == 0:
    # Если год не високосный, но делится на 100 без остатка, проверяем, делится ли он на 4
    print(f"Год {year} не является високосным.")
elif year % 4 == 0:
    # Если год не делится на 100 и делится на 4, то он високосный
    print(f"Год {year} является високосным.")
else:
    # Во всех остальных случаях год не високосный
    print(f"Год {year} не является високосным.")
 """